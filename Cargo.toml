[package]
name = "portwatch"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Watch TCP/UDP ports and show listening services"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
